<Window x:Class="BoardZWtfClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:boardZWtfClient="clr-namespace:BoardZWtfClient"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="BoardZ WTF Client" Height="350" Width="525" d:DataContext="{d:DesignInstance boardZWtfClient:MainWindow}">
    <Grid>
       <Grid.RowDefinitions>
           <RowDefinition Height="Auto"></RowDefinition>
           <RowDefinition></RowDefinition>
       </Grid.RowDefinitions>
        <Button Grid.Row="0" Grid.Column="0" Click="RequestDataClicked">Request Data</Button>
        <ListBox Grid.Row="1" Grid.Column="0" ItemsSource="{Binding BoardGames}">
            <ListBox.ItemTemplate><DataTemplate>
                <TextBlock>
                    <Run Text="{Binding Name}"></Run>
                    <Run> (</Run>
                    <Run Text="{Binding Description}"></Run>
                    <Run>)</Run>
                </TextBlock>
            </DataTemplate></ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
